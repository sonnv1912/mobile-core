export const initI18n = (resources: any) => {
   // i18n.use(initReactI18next).init({
   //    resources,
   //    lng: 'en',
   //    interpolation: {
   //       escapeValue: false,
   //    },
   // });

   return i18n;
};
