import { Screen } from '@components/ui/screen';
import { Text } from 'react-native';

export const DrawerContent = () => {
   return (
      <Screen>
         <Text>hi</Text>
      </Screen>
   );
};
